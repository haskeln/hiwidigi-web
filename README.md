# HiwiDigi Web

Apple-style landing + interactive demo.

## Local preview
Open `hiwi-moat-demo.html` in a browser, or run a static server.

## Live API
The demo calls `https://api.hiwidigi.com/run-intent` in production and falls back to `http://localhost:5174` locally.
